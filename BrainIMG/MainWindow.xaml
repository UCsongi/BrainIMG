<Window x:Class="BrainIMG.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        DataContext="{Binding}"
        mc:Ignorable="d"
        Title="MainWindow" Height="Auto" Width="Auto">
    <StackPanel>
        <StackPanel Orientation="Horizontal" Background="LightBlue" Height="40">
            <Label Margin="10,0,0,0" Height="23" Name="Label1">
                Data location:
            </Label>
            <Label Margin="5,0,0,0" Height="25" Name="FileNameLabel" Width="300" Content="{Binding FolderPath, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Margin="5,0,0,0" Height="23" Name="BrowseButton" Width="75" Click="FolderBrowseButtonClick">
                Browse
            </Button>
        </StackPanel>
        <Button Content="Import Data" Click="ImportButtonClick"></Button>
        <Button Content="Display images" Click="DisplayButtonClick"></Button>
    </StackPanel>
</Window>
